const newMeetingPoint = closest startpoint
const totalDist1 = dist to initial meetingPoint
const totalDist2 = calc total distance to newMeetingPoint

if(co2EmissionsPerKM * (totalDist2-totalDist) < LTO){
	setMeetingPoint(newMeetingPoint)
} else {
	return
}